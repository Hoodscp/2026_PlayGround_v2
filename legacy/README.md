# Legacy prototype

This directory preserves the original static HTML and DOM-script prototype for
visual reference. The Next.js application does not import or serve these files.
All active page markup and interactions live under `app/components`.
